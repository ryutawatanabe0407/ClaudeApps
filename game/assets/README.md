# ディッキア画像の配置方法

このディレクトリに、ディッキアの画像を `dyckia.png` という名前で保存してください。

## 手順

1. ディッキアの写真を `dyckia.png` として保存
2. このディレクトリ（`game/assets/`）に配置
3. ゲームをリロードすると、画像が使用されます

## 画像の推奨サイズ

- 推奨サイズ: 200x200px 以上
- フォーマット: PNG, JPG, WebP
- 背景: 透明または白

画像が見つからない場合は、自動的に絵文字（🌵）が使用されます。
